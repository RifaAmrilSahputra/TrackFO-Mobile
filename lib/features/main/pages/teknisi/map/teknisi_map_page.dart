import 'package:flutter/material.dart';

class TeknisiMapPage extends StatelessWidget {
  const TeknisiMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Teknisi - Map')));
  }
}
